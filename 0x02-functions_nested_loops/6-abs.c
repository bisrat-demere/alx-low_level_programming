#include "main.h"
/**
 * _abs - number
 * description - compute abs value
 * Return: always 0
**/

int _abs(int)
{
	int r;

	if (r > 0)
	{
		_putchar(p);
		return (1);
	}
	else if (n < 0)
	{
		_putchar(nn);
		return (-1);
	}
	else
	{
		_putchar(z);
		return (0);
	}
}
