#include "main.h"
/**
 * mul - multiplication
 * @a: input one
 * @b: input two
 * description - return 1 if c is u.c
 * Return: mult
**/

int mul(int a, int b)
{
	int mult;

	mult = (a * b);
	return (mult);
}
